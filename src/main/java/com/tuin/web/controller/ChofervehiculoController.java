/*
 * Created on 28 ago 2017 ( Time 17:51:38 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Chofervehiculo;
import com.tuin.bean.Chofer;
import com.tuin.bean.Vehiculo;

//--- Services 
import com.tuin.business.service.ChofervehiculoService;
import com.tuin.business.service.ChoferService;
import com.tuin.business.service.VehiculoService;

//--- List Items 
import com.tuin.web.listitem.ChoferListItem;
import com.tuin.web.listitem.VehiculoListItem;

/**
 * Spring MVC controller for 'Chofervehiculo' management.
 */
@Controller
@RequestMapping("/chofervehiculo")
public class ChofervehiculoController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "chofervehiculo";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "chofervehiculo/form";
	private static final String JSP_LIST   = "chofervehiculo/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/chofervehiculo/create";
	private static final String SAVE_ACTION_UPDATE   = "/chofervehiculo/update";

	//--- Main entity service
	@Resource
    private ChofervehiculoService chofervehiculoService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private ChoferService choferService; // Injected by Spring
	@Resource
    private VehiculoService vehiculoService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ChofervehiculoController() {
		super(ChofervehiculoController.class, MAIN_ENTITY_NAME );
		log("ChofervehiculoController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Chofer"
	 * @param model
	 */
	private void populateListOfChoferItems(Model model) {
		List<Chofer> list = choferService.findAll();
		List<ChoferListItem> items = new LinkedList<ChoferListItem>();
		for ( Chofer chofer : list ) {
			items.add(new ChoferListItem( chofer ) );
		}
		model.addAttribute("listOfChoferItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Vehiculo"
	 * @param model
	 */
	private void populateListOfVehiculoItems(Model model) {
		List<Vehiculo> list = vehiculoService.findAll();
		List<VehiculoListItem> items = new LinkedList<VehiculoListItem>();
		for ( Vehiculo vehiculo : list ) {
			items.add(new VehiculoListItem( vehiculo ) );
		}
		model.addAttribute("listOfVehiculoItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param chofervehiculo
	 */
	private void populateModel(Model model, Chofervehiculo chofervehiculo, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, chofervehiculo);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfChoferItems(model);
			populateListOfVehiculoItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfChoferItems(model);
			populateListOfVehiculoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Chofervehiculo found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Chofervehiculo> list = chofervehiculoService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Chofervehiculo
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Chofervehiculo chofervehiculo = new Chofervehiculo();	
		populateModel( model, chofervehiculo, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Chofervehiculo
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Chofervehiculo chofervehiculo = chofervehiculoService.findById(id);
		populateModel( model, chofervehiculo, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param chofervehiculo  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Chofervehiculo chofervehiculo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Chofervehiculo chofervehiculoCreated = chofervehiculoService.create(chofervehiculo); 
				model.addAttribute(MAIN_ENTITY_NAME, chofervehiculoCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, chofervehiculo.getId() );
			} else {
				populateModel( model, chofervehiculo, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "chofervehiculo.error.create", e);
			populateModel( model, chofervehiculo, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param chofervehiculo  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Chofervehiculo chofervehiculo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Chofervehiculo chofervehiculoSaved = chofervehiculoService.update(chofervehiculo);
				model.addAttribute(MAIN_ENTITY_NAME, chofervehiculoSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, chofervehiculo.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, chofervehiculo, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "chofervehiculo.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, chofervehiculo, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			chofervehiculoService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "chofervehiculo.error.delete", e);
		}
		return redirectToList();
	}

}
