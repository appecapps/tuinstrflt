/*
 * Created on 28 ago 2017 ( Time 17:51:39 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Clienteviaje;
import com.tuin.bean.Viaje;
import com.tuin.bean.Cliente;
import com.tuin.bean.Estado;

//--- Services 
import com.tuin.business.service.ClienteviajeService;
import com.tuin.business.service.ViajeService;
import com.tuin.business.service.ClienteService;
import com.tuin.business.service.EstadoService;

//--- List Items 
import com.tuin.web.listitem.ViajeListItem;
import com.tuin.web.listitem.ClienteListItem;
import com.tuin.web.listitem.EstadoListItem;

/**
 * Spring MVC controller for 'Clienteviaje' management.
 */
@Controller
@RequestMapping("/clienteviaje")
public class ClienteviajeController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "clienteviaje";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "clienteviaje/form";
	private static final String JSP_LIST   = "clienteviaje/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/clienteviaje/create";
	private static final String SAVE_ACTION_UPDATE   = "/clienteviaje/update";

	//--- Main entity service
	@Resource
    private ClienteviajeService clienteviajeService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private ViajeService viajeService; // Injected by Spring
	@Resource
    private ClienteService clienteService; // Injected by Spring
	@Resource
    private EstadoService estadoService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ClienteviajeController() {
		super(ClienteviajeController.class, MAIN_ENTITY_NAME );
		log("ClienteviajeController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Viaje"
	 * @param model
	 */
	private void populateListOfViajeItems(Model model) {
		List<Viaje> list = viajeService.findAll();
		List<ViajeListItem> items = new LinkedList<ViajeListItem>();
		for ( Viaje viaje : list ) {
			items.add(new ViajeListItem( viaje ) );
		}
		model.addAttribute("listOfViajeItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Cliente"
	 * @param model
	 */
	private void populateListOfClienteItems(Model model) {
		List<Cliente> list = clienteService.findAll();
		List<ClienteListItem> items = new LinkedList<ClienteListItem>();
		for ( Cliente cliente : list ) {
			items.add(new ClienteListItem( cliente ) );
		}
		model.addAttribute("listOfClienteItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Estado"
	 * @param model
	 */
	private void populateListOfEstadoItems(Model model) {
		List<Estado> list = estadoService.findAll();
		List<EstadoListItem> items = new LinkedList<EstadoListItem>();
		for ( Estado estado : list ) {
			items.add(new EstadoListItem( estado ) );
		}
		model.addAttribute("listOfEstadoItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param clienteviaje
	 */
	private void populateModel(Model model, Clienteviaje clienteviaje, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, clienteviaje);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfViajeItems(model);
			populateListOfClienteItems(model);
			populateListOfEstadoItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfViajeItems(model);
			populateListOfClienteItems(model);
			populateListOfEstadoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Clienteviaje found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Clienteviaje> list = clienteviajeService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Clienteviaje
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Clienteviaje clienteviaje = new Clienteviaje();	
		populateModel( model, clienteviaje, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Clienteviaje
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Clienteviaje clienteviaje = clienteviajeService.findById(id);
		populateModel( model, clienteviaje, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param clienteviaje  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Clienteviaje clienteviaje, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Clienteviaje clienteviajeCreated = clienteviajeService.create(clienteviaje); 
				model.addAttribute(MAIN_ENTITY_NAME, clienteviajeCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, clienteviaje.getId() );
			} else {
				populateModel( model, clienteviaje, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "clienteviaje.error.create", e);
			populateModel( model, clienteviaje, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param clienteviaje  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Clienteviaje clienteviaje, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Clienteviaje clienteviajeSaved = clienteviajeService.update(clienteviaje);
				model.addAttribute(MAIN_ENTITY_NAME, clienteviajeSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, clienteviaje.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, clienteviaje, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "clienteviaje.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, clienteviaje, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			clienteviajeService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "clienteviaje.error.delete", e);
		}
		return redirectToList();
	}

}
