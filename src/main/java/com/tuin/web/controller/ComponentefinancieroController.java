/*
 * Created on 28 ago 2017 ( Time 17:51:40 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Componentefinanciero;
import com.tuin.bean.Tipodocumento;
import com.tuin.bean.Tipocomponentefinanciero;

//--- Services 
import com.tuin.business.service.ComponentefinancieroService;
import com.tuin.business.service.TipodocumentoService;
import com.tuin.business.service.TipocomponentefinancieroService;

//--- List Items 
import com.tuin.web.listitem.TipodocumentoListItem;
import com.tuin.web.listitem.TipocomponentefinancieroListItem;

/**
 * Spring MVC controller for 'Componentefinanciero' management.
 */
@Controller
@RequestMapping("/componentefinanciero")
public class ComponentefinancieroController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "componentefinanciero";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "componentefinanciero/form";
	private static final String JSP_LIST   = "componentefinanciero/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/componentefinanciero/create";
	private static final String SAVE_ACTION_UPDATE   = "/componentefinanciero/update";

	//--- Main entity service
	@Resource
    private ComponentefinancieroService componentefinancieroService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private TipodocumentoService tipodocumentoService; // Injected by Spring
	@Resource
    private TipocomponentefinancieroService tipocomponentefinancieroService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ComponentefinancieroController() {
		super(ComponentefinancieroController.class, MAIN_ENTITY_NAME );
		log("ComponentefinancieroController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Tipodocumento"
	 * @param model
	 */
	private void populateListOfTipodocumentoItems(Model model) {
		List<Tipodocumento> list = tipodocumentoService.findAll();
		List<TipodocumentoListItem> items = new LinkedList<TipodocumentoListItem>();
		for ( Tipodocumento tipodocumento : list ) {
			items.add(new TipodocumentoListItem( tipodocumento ) );
		}
		model.addAttribute("listOfTipodocumentoItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Tipocomponentefinanciero"
	 * @param model
	 */
	private void populateListOfTipocomponentefinancieroItems(Model model) {
		List<Tipocomponentefinanciero> list = tipocomponentefinancieroService.findAll();
		List<TipocomponentefinancieroListItem> items = new LinkedList<TipocomponentefinancieroListItem>();
		for ( Tipocomponentefinanciero tipocomponentefinanciero : list ) {
			items.add(new TipocomponentefinancieroListItem( tipocomponentefinanciero ) );
		}
		model.addAttribute("listOfTipocomponentefinancieroItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param componentefinanciero
	 */
	private void populateModel(Model model, Componentefinanciero componentefinanciero, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, componentefinanciero);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfTipodocumentoItems(model);
			populateListOfTipocomponentefinancieroItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfTipodocumentoItems(model);
			populateListOfTipocomponentefinancieroItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Componentefinanciero found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Componentefinanciero> list = componentefinancieroService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Componentefinanciero
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Componentefinanciero componentefinanciero = new Componentefinanciero();	
		populateModel( model, componentefinanciero, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Componentefinanciero
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Componentefinanciero componentefinanciero = componentefinancieroService.findById(id);
		populateModel( model, componentefinanciero, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param componentefinanciero  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Componentefinanciero componentefinanciero, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Componentefinanciero componentefinancieroCreated = componentefinancieroService.create(componentefinanciero); 
				model.addAttribute(MAIN_ENTITY_NAME, componentefinancieroCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, componentefinanciero.getId() );
			} else {
				populateModel( model, componentefinanciero, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "componentefinanciero.error.create", e);
			populateModel( model, componentefinanciero, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param componentefinanciero  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Componentefinanciero componentefinanciero, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Componentefinanciero componentefinancieroSaved = componentefinancieroService.update(componentefinanciero);
				model.addAttribute(MAIN_ENTITY_NAME, componentefinancieroSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, componentefinanciero.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, componentefinanciero, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "componentefinanciero.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, componentefinanciero, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			componentefinancieroService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "componentefinanciero.error.delete", e);
		}
		return redirectToList();
	}

}
