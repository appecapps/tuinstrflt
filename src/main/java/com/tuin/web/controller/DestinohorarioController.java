/*
 * Created on 28 ago 2017 ( Time 17:51:40 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Destinohorario;
import com.tuin.bean.Ciudad;
import com.tuin.bean.Horario;

//--- Services 
import com.tuin.business.service.DestinohorarioService;
import com.tuin.business.service.CiudadService;
import com.tuin.business.service.HorarioService;

//--- List Items 
import com.tuin.web.listitem.CiudadListItem;
import com.tuin.web.listitem.HorarioListItem;

/**
 * Spring MVC controller for 'Destinohorario' management.
 */
@Controller
@RequestMapping("/destinohorario")
public class DestinohorarioController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "destinohorario";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "destinohorario/form";
	private static final String JSP_LIST   = "destinohorario/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/destinohorario/create";
	private static final String SAVE_ACTION_UPDATE   = "/destinohorario/update";

	//--- Main entity service
	@Resource
    private DestinohorarioService destinohorarioService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private CiudadService ciudadService; // Injected by Spring
	@Resource
    private HorarioService horarioService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public DestinohorarioController() {
		super(DestinohorarioController.class, MAIN_ENTITY_NAME );
		log("DestinohorarioController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Ciudad"
	 * @param model
	 */
	private void populateListOfCiudadItems(Model model) {
		List<Ciudad> list = ciudadService.findAll();
		List<CiudadListItem> items = new LinkedList<CiudadListItem>();
		for ( Ciudad ciudad : list ) {
			items.add(new CiudadListItem( ciudad ) );
		}
		model.addAttribute("listOfCiudadItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Horario"
	 * @param model
	 */
	private void populateListOfHorarioItems(Model model) {
		List<Horario> list = horarioService.findAll();
		List<HorarioListItem> items = new LinkedList<HorarioListItem>();
		for ( Horario horario : list ) {
			items.add(new HorarioListItem( horario ) );
		}
		model.addAttribute("listOfHorarioItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param destinohorario
	 */
	private void populateModel(Model model, Destinohorario destinohorario, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, destinohorario);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfCiudadItems(model);
			populateListOfHorarioItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfHorarioItems(model);
			populateListOfCiudadItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Destinohorario found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Destinohorario> list = destinohorarioService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Destinohorario
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Destinohorario destinohorario = new Destinohorario();	
		populateModel( model, destinohorario, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Destinohorario
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Destinohorario destinohorario = destinohorarioService.findById(id);
		populateModel( model, destinohorario, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param destinohorario  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Destinohorario destinohorario, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Destinohorario destinohorarioCreated = destinohorarioService.create(destinohorario); 
				model.addAttribute(MAIN_ENTITY_NAME, destinohorarioCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, destinohorario.getId() );
			} else {
				populateModel( model, destinohorario, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "destinohorario.error.create", e);
			populateModel( model, destinohorario, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param destinohorario  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Destinohorario destinohorario, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Destinohorario destinohorarioSaved = destinohorarioService.update(destinohorario);
				model.addAttribute(MAIN_ENTITY_NAME, destinohorarioSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, destinohorario.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, destinohorario, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "destinohorario.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, destinohorario, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			destinohorarioService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "destinohorario.error.delete", e);
		}
		return redirectToList();
	}

}
