/*
 * Created on 28 ago 2017 ( Time 17:51:40 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Documento;
import com.tuin.bean.Estado;
import com.tuin.bean.Tipodocumento;

//--- Services 
import com.tuin.business.service.DocumentoService;
import com.tuin.business.service.EstadoService;
import com.tuin.business.service.TipodocumentoService;

//--- List Items 
import com.tuin.web.listitem.EstadoListItem;
import com.tuin.web.listitem.TipodocumentoListItem;

/**
 * Spring MVC controller for 'Documento' management.
 */
@Controller
@RequestMapping("/documento")
public class DocumentoController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "documento";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "documento/form";
	private static final String JSP_LIST   = "documento/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/documento/create";
	private static final String SAVE_ACTION_UPDATE   = "/documento/update";

	//--- Main entity service
	@Resource
    private DocumentoService documentoService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private EstadoService estadoService; // Injected by Spring
	@Resource
    private TipodocumentoService tipodocumentoService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public DocumentoController() {
		super(DocumentoController.class, MAIN_ENTITY_NAME );
		log("DocumentoController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Estado"
	 * @param model
	 */
	private void populateListOfEstadoItems(Model model) {
		List<Estado> list = estadoService.findAll();
		List<EstadoListItem> items = new LinkedList<EstadoListItem>();
		for ( Estado estado : list ) {
			items.add(new EstadoListItem( estado ) );
		}
		model.addAttribute("listOfEstadoItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Tipodocumento"
	 * @param model
	 */
	private void populateListOfTipodocumentoItems(Model model) {
		List<Tipodocumento> list = tipodocumentoService.findAll();
		List<TipodocumentoListItem> items = new LinkedList<TipodocumentoListItem>();
		for ( Tipodocumento tipodocumento : list ) {
			items.add(new TipodocumentoListItem( tipodocumento ) );
		}
		model.addAttribute("listOfTipodocumentoItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param documento
	 */
	private void populateModel(Model model, Documento documento, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, documento);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfEstadoItems(model);
			populateListOfTipodocumentoItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfEstadoItems(model);
			populateListOfTipodocumentoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Documento found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Documento> list = documentoService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Documento
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Documento documento = new Documento();	
		populateModel( model, documento, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Documento
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Documento documento = documentoService.findById(id);
		populateModel( model, documento, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param documento  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Documento documento, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Documento documentoCreated = documentoService.create(documento); 
				model.addAttribute(MAIN_ENTITY_NAME, documentoCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, documento.getId() );
			} else {
				populateModel( model, documento, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "documento.error.create", e);
			populateModel( model, documento, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param documento  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Documento documento, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Documento documentoSaved = documentoService.update(documento);
				model.addAttribute(MAIN_ENTITY_NAME, documentoSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, documento.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, documento, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "documento.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, documento, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			documentoService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "documento.error.delete", e);
		}
		return redirectToList();
	}

}
