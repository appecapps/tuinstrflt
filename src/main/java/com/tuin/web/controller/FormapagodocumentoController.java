/*
 * Created on 28 ago 2017 ( Time 17:51:42 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Formapagodocumento;
import com.tuin.bean.Documento;
import com.tuin.bean.Formapago;

//--- Services 
import com.tuin.business.service.FormapagodocumentoService;
import com.tuin.business.service.DocumentoService;
import com.tuin.business.service.FormapagoService;

//--- List Items 
import com.tuin.web.listitem.DocumentoListItem;
import com.tuin.web.listitem.FormapagoListItem;

/**
 * Spring MVC controller for 'Formapagodocumento' management.
 */
@Controller
@RequestMapping("/formapagodocumento")
public class FormapagodocumentoController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "formapagodocumento";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "formapagodocumento/form";
	private static final String JSP_LIST   = "formapagodocumento/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/formapagodocumento/create";
	private static final String SAVE_ACTION_UPDATE   = "/formapagodocumento/update";

	//--- Main entity service
	@Resource
    private FormapagodocumentoService formapagodocumentoService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private DocumentoService documentoService; // Injected by Spring
	@Resource
    private FormapagoService formapagoService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public FormapagodocumentoController() {
		super(FormapagodocumentoController.class, MAIN_ENTITY_NAME );
		log("FormapagodocumentoController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Documento"
	 * @param model
	 */
	private void populateListOfDocumentoItems(Model model) {
		List<Documento> list = documentoService.findAll();
		List<DocumentoListItem> items = new LinkedList<DocumentoListItem>();
		for ( Documento documento : list ) {
			items.add(new DocumentoListItem( documento ) );
		}
		model.addAttribute("listOfDocumentoItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Formapago"
	 * @param model
	 */
	private void populateListOfFormapagoItems(Model model) {
		List<Formapago> list = formapagoService.findAll();
		List<FormapagoListItem> items = new LinkedList<FormapagoListItem>();
		for ( Formapago formapago : list ) {
			items.add(new FormapagoListItem( formapago ) );
		}
		model.addAttribute("listOfFormapagoItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param formapagodocumento
	 */
	private void populateModel(Model model, Formapagodocumento formapagodocumento, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, formapagodocumento);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfDocumentoItems(model);
			populateListOfFormapagoItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfDocumentoItems(model);
			populateListOfFormapagoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Formapagodocumento found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Formapagodocumento> list = formapagodocumentoService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Formapagodocumento
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Formapagodocumento formapagodocumento = new Formapagodocumento();	
		populateModel( model, formapagodocumento, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Formapagodocumento
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Formapagodocumento formapagodocumento = formapagodocumentoService.findById(id);
		populateModel( model, formapagodocumento, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param formapagodocumento  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Formapagodocumento formapagodocumento, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Formapagodocumento formapagodocumentoCreated = formapagodocumentoService.create(formapagodocumento); 
				model.addAttribute(MAIN_ENTITY_NAME, formapagodocumentoCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, formapagodocumento.getId() );
			} else {
				populateModel( model, formapagodocumento, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "formapagodocumento.error.create", e);
			populateModel( model, formapagodocumento, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param formapagodocumento  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Formapagodocumento formapagodocumento, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Formapagodocumento formapagodocumentoSaved = formapagodocumentoService.update(formapagodocumento);
				model.addAttribute(MAIN_ENTITY_NAME, formapagodocumentoSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, formapagodocumento.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, formapagodocumento, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "formapagodocumento.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, formapagodocumento, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			formapagodocumentoService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "formapagodocumento.error.delete", e);
		}
		return redirectToList();
	}

}
