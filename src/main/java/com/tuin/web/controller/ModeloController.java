/*
 * Created on 28 ago 2017 ( Time 17:51:44 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Modelo;
import com.tuin.bean.Marca;
import com.tuin.bean.Tipovehiculo;

//--- Services 
import com.tuin.business.service.ModeloService;
import com.tuin.business.service.MarcaService;
import com.tuin.business.service.TipovehiculoService;

//--- List Items 
import com.tuin.web.listitem.MarcaListItem;
import com.tuin.web.listitem.TipovehiculoListItem;

/**
 * Spring MVC controller for 'Modelo' management.
 */
@Controller
@RequestMapping("/modelo")
public class ModeloController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "modelo";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "modelo/form";
	private static final String JSP_LIST   = "modelo/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/modelo/create";
	private static final String SAVE_ACTION_UPDATE   = "/modelo/update";

	//--- Main entity service
	@Resource
    private ModeloService modeloService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private MarcaService marcaService; // Injected by Spring
	@Resource
    private TipovehiculoService tipovehiculoService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ModeloController() {
		super(ModeloController.class, MAIN_ENTITY_NAME );
		log("ModeloController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Marca"
	 * @param model
	 */
	private void populateListOfMarcaItems(Model model) {
		List<Marca> list = marcaService.findAll();
		List<MarcaListItem> items = new LinkedList<MarcaListItem>();
		for ( Marca marca : list ) {
			items.add(new MarcaListItem( marca ) );
		}
		model.addAttribute("listOfMarcaItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Tipovehiculo"
	 * @param model
	 */
	private void populateListOfTipovehiculoItems(Model model) {
		List<Tipovehiculo> list = tipovehiculoService.findAll();
		List<TipovehiculoListItem> items = new LinkedList<TipovehiculoListItem>();
		for ( Tipovehiculo tipovehiculo : list ) {
			items.add(new TipovehiculoListItem( tipovehiculo ) );
		}
		model.addAttribute("listOfTipovehiculoItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param modelo
	 */
	private void populateModel(Model model, Modelo modelo, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, modelo);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfMarcaItems(model);
			populateListOfTipovehiculoItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfMarcaItems(model);
			populateListOfTipovehiculoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Modelo found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Modelo> list = modeloService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Modelo
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Modelo modelo = new Modelo();	
		populateModel( model, modelo, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Modelo
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Modelo modelo = modeloService.findById(id);
		populateModel( model, modelo, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param modelo  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Modelo modelo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Modelo modeloCreated = modeloService.create(modelo); 
				model.addAttribute(MAIN_ENTITY_NAME, modeloCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, modelo.getId() );
			} else {
				populateModel( model, modelo, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "modelo.error.create", e);
			populateModel( model, modelo, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param modelo  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Modelo modelo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Modelo modeloSaved = modeloService.update(modelo);
				model.addAttribute(MAIN_ENTITY_NAME, modeloSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, modelo.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, modelo, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "modelo.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, modelo, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			modeloService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "modelo.error.delete", e);
		}
		return redirectToList();
	}

}
