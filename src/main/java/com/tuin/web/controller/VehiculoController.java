/*
 * Created on 28 ago 2017 ( Time 17:51:47 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Vehiculo;
import com.tuin.bean.Modelo;
import com.tuin.bean.Color;

//--- Services 
import com.tuin.business.service.VehiculoService;
import com.tuin.business.service.ModeloService;
import com.tuin.business.service.ColorService;

//--- List Items 
import com.tuin.web.listitem.ModeloListItem;
import com.tuin.web.listitem.ColorListItem;

/**
 * Spring MVC controller for 'Vehiculo' management.
 */
@Controller
@RequestMapping("/vehiculo")
public class VehiculoController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "vehiculo";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "vehiculo/form";
	private static final String JSP_LIST   = "vehiculo/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/vehiculo/create";
	private static final String SAVE_ACTION_UPDATE   = "/vehiculo/update";

	//--- Main entity service
	@Resource
    private VehiculoService vehiculoService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private ModeloService modeloService; // Injected by Spring
	@Resource
    private ColorService colorService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public VehiculoController() {
		super(VehiculoController.class, MAIN_ENTITY_NAME );
		log("VehiculoController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Modelo"
	 * @param model
	 */
	private void populateListOfModeloItems(Model model) {
		List<Modelo> list = modeloService.findAll();
		List<ModeloListItem> items = new LinkedList<ModeloListItem>();
		for ( Modelo modelo : list ) {
			items.add(new ModeloListItem( modelo ) );
		}
		model.addAttribute("listOfModeloItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Color"
	 * @param model
	 */
	private void populateListOfColorItems(Model model) {
		List<Color> list = colorService.findAll();
		List<ColorListItem> items = new LinkedList<ColorListItem>();
		for ( Color color : list ) {
			items.add(new ColorListItem( color ) );
		}
		model.addAttribute("listOfColorItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param vehiculo
	 */
	private void populateModel(Model model, Vehiculo vehiculo, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, vehiculo);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfModeloItems(model);
			populateListOfColorItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfModeloItems(model);
			populateListOfColorItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Vehiculo found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Vehiculo> list = vehiculoService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Vehiculo
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Vehiculo vehiculo = new Vehiculo();	
		populateModel( model, vehiculo, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Vehiculo
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Vehiculo vehiculo = vehiculoService.findById(id);
		populateModel( model, vehiculo, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param vehiculo  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Vehiculo vehiculo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Vehiculo vehiculoCreated = vehiculoService.create(vehiculo); 
				model.addAttribute(MAIN_ENTITY_NAME, vehiculoCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, vehiculo.getId() );
			} else {
				populateModel( model, vehiculo, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "vehiculo.error.create", e);
			populateModel( model, vehiculo, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param vehiculo  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Vehiculo vehiculo, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Vehiculo vehiculoSaved = vehiculoService.update(vehiculo);
				model.addAttribute(MAIN_ENTITY_NAME, vehiculoSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, vehiculo.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, vehiculo, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "vehiculo.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, vehiculo, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			vehiculoService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "vehiculo.error.delete", e);
		}
		return redirectToList();
	}

}
