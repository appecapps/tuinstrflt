/*
 * Created on 28 ago 2017 ( Time 17:51:48 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.tuin.web.common.AbstractController;
import com.tuin.web.common.FormMode;
import com.tuin.web.common.Message;
import com.tuin.web.common.MessageType;

//--- Entities
import com.tuin.bean.Viaje;
import com.tuin.bean.Chofervehiculo;
import com.tuin.bean.Destinohorario;

//--- Services 
import com.tuin.business.service.ViajeService;
import com.tuin.business.service.ChofervehiculoService;
import com.tuin.business.service.DestinohorarioService;

//--- List Items 
import com.tuin.web.listitem.ChofervehiculoListItem;
import com.tuin.web.listitem.DestinohorarioListItem;

/**
 * Spring MVC controller for 'Viaje' management.
 */
@Controller
@RequestMapping("/viaje")
public class ViajeController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "viaje";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "viaje/form";
	private static final String JSP_LIST   = "viaje/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/viaje/create";
	private static final String SAVE_ACTION_UPDATE   = "/viaje/update";

	//--- Main entity service
	@Resource
    private ViajeService viajeService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private ChofervehiculoService chofervehiculoService; // Injected by Spring
	@Resource
    private DestinohorarioService destinohorarioService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ViajeController() {
		super(ViajeController.class, MAIN_ENTITY_NAME );
		log("ViajeController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Chofervehiculo"
	 * @param model
	 */
	private void populateListOfChofervehiculoItems(Model model) {
		List<Chofervehiculo> list = chofervehiculoService.findAll();
		List<ChofervehiculoListItem> items = new LinkedList<ChofervehiculoListItem>();
		for ( Chofervehiculo chofervehiculo : list ) {
			items.add(new ChofervehiculoListItem( chofervehiculo ) );
		}
		model.addAttribute("listOfChofervehiculoItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Destinohorario"
	 * @param model
	 */
	private void populateListOfDestinohorarioItems(Model model) {
		List<Destinohorario> list = destinohorarioService.findAll();
		List<DestinohorarioListItem> items = new LinkedList<DestinohorarioListItem>();
		for ( Destinohorario destinohorario : list ) {
			items.add(new DestinohorarioListItem( destinohorario ) );
		}
		model.addAttribute("listOfDestinohorarioItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param viaje
	 */
	private void populateModel(Model model, Viaje viaje, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, viaje);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfChofervehiculoItems(model);
			populateListOfDestinohorarioItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfDestinohorarioItems(model);
			populateListOfChofervehiculoItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Viaje found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Viaje> list = viajeService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Viaje
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Viaje viaje = new Viaje();	
		populateModel( model, viaje, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Viaje
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Long id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Viaje viaje = viajeService.findById(id);
		populateModel( model, viaje, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param viaje  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Viaje viaje, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Viaje viajeCreated = viajeService.create(viaje); 
				model.addAttribute(MAIN_ENTITY_NAME, viajeCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, viaje.getId() );
			} else {
				populateModel( model, viaje, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "viaje.error.create", e);
			populateModel( model, viaje, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param viaje  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Viaje viaje, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Viaje viajeSaved = viajeService.update(viaje);
				model.addAttribute(MAIN_ENTITY_NAME, viajeSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, viaje.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, viaje, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "viaje.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, viaje, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Long id) {
		log("Action 'delete'" );
		try {
			viajeService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "viaje.error.delete", e);
		}
		return redirectToList();
	}

}
