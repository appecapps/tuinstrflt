/*
 * Created on 28 ago 2017 ( Time 17:51:13 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.business.service.mapping;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.modelmapper.ModelMapper;
import org.modelmapper.convention.MatchingStrategies;
import org.springframework.stereotype.Component;
import com.tuin.bean.Color;
import com.tuin.bean.jpa.ColorEntity;
import com.tuin.test.MockValues;

/**
 * Test : Mapping between entity beans and display beans.
 */
public class ColorServiceMapperTest {

	private ColorServiceMapper colorServiceMapper;

	private static ModelMapper modelMapper = new ModelMapper();

	private MockValues mockValues = new MockValues();
	
	
	@BeforeClass
	public static void setUp() {
		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
	}
	
	@Before
	public void before() {
		colorServiceMapper = new ColorServiceMapper();
		colorServiceMapper.setModelMapper(modelMapper);
	}
	
	/**
	 * Mapping from 'ColorEntity' to 'Color'
	 * @param colorEntity
	 */
	@Test
	public void testMapColorEntityToColor() {
		// Given
		ColorEntity colorEntity = new ColorEntity();
		colorEntity.setNombre(mockValues.nextString(45));
		colorEntity.setActivo(mockValues.nextByte());
		
		// When
		Color color = colorServiceMapper.mapColorEntityToColor(colorEntity);
		
		// Then
		assertEquals(colorEntity.getNombre(), color.getNombre());
		assertEquals(colorEntity.getActivo(), color.getActivo());
	}
	
	/**
	 * Test : Mapping from 'Color' to 'ColorEntity'
	 */
	@Test
	public void testMapColorToColorEntity() {
		// Given
		Color color = new Color();
		color.setNombre(mockValues.nextString(45));
		color.setActivo(mockValues.nextByte());

		ColorEntity colorEntity = new ColorEntity();
		
		// When
		colorServiceMapper.mapColorToColorEntity(color, colorEntity);
		
		// Then
		assertEquals(color.getNombre(), colorEntity.getNombre());
		assertEquals(color.getActivo(), colorEntity.getActivo());
	}

}