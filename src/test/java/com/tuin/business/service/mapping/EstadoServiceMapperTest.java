/*
 * Created on 28 ago 2017 ( Time 17:51:15 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.business.service.mapping;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.modelmapper.ModelMapper;
import org.modelmapper.convention.MatchingStrategies;
import org.springframework.stereotype.Component;
import com.tuin.bean.Estado;
import com.tuin.bean.jpa.EstadoEntity;
import com.tuin.test.MockValues;

/**
 * Test : Mapping between entity beans and display beans.
 */
public class EstadoServiceMapperTest {

	private EstadoServiceMapper estadoServiceMapper;

	private static ModelMapper modelMapper = new ModelMapper();

	private MockValues mockValues = new MockValues();
	
	
	@BeforeClass
	public static void setUp() {
		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
	}
	
	@Before
	public void before() {
		estadoServiceMapper = new EstadoServiceMapper();
		estadoServiceMapper.setModelMapper(modelMapper);
	}
	
	/**
	 * Mapping from 'EstadoEntity' to 'Estado'
	 * @param estadoEntity
	 */
	@Test
	public void testMapEstadoEntityToEstado() {
		// Given
		EstadoEntity estadoEntity = new EstadoEntity();
		estadoEntity.setNombre(mockValues.nextString(45));
		estadoEntity.setTabla(mockValues.nextString(45));
		
		// When
		Estado estado = estadoServiceMapper.mapEstadoEntityToEstado(estadoEntity);
		
		// Then
		assertEquals(estadoEntity.getNombre(), estado.getNombre());
		assertEquals(estadoEntity.getTabla(), estado.getTabla());
	}
	
	/**
	 * Test : Mapping from 'Estado' to 'EstadoEntity'
	 */
	@Test
	public void testMapEstadoToEstadoEntity() {
		// Given
		Estado estado = new Estado();
		estado.setNombre(mockValues.nextString(45));
		estado.setTabla(mockValues.nextString(45));

		EstadoEntity estadoEntity = new EstadoEntity();
		
		// When
		estadoServiceMapper.mapEstadoToEstadoEntity(estado, estadoEntity);
		
		// Then
		assertEquals(estado.getNombre(), estadoEntity.getNombre());
		assertEquals(estado.getTabla(), estadoEntity.getTabla());
	}

}