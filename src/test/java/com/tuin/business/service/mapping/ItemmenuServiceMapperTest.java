/*
 * Created on 28 ago 2017 ( Time 17:51:17 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.tuin.business.service.mapping;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.modelmapper.ModelMapper;
import org.modelmapper.convention.MatchingStrategies;
import org.springframework.stereotype.Component;
import com.tuin.bean.Itemmenu;
import com.tuin.bean.jpa.ItemmenuEntity;
import com.tuin.bean.jpa.MenuEntity;
import com.tuin.test.MockValues;

/**
 * Test : Mapping between entity beans and display beans.
 */
public class ItemmenuServiceMapperTest {

	private ItemmenuServiceMapper itemmenuServiceMapper;

	private static ModelMapper modelMapper = new ModelMapper();

	private MockValues mockValues = new MockValues();
	
	
	@BeforeClass
	public static void setUp() {
		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
	}
	
	@Before
	public void before() {
		itemmenuServiceMapper = new ItemmenuServiceMapper();
		itemmenuServiceMapper.setModelMapper(modelMapper);
	}
	
	/**
	 * Mapping from 'ItemmenuEntity' to 'Itemmenu'
	 * @param itemmenuEntity
	 */
	@Test
	public void testMapItemmenuEntityToItemmenu() {
		// Given
		ItemmenuEntity itemmenuEntity = new ItemmenuEntity();
		itemmenuEntity.setLink(mockValues.nextString(45));
		itemmenuEntity.setNombre(mockValues.nextString(45));
		itemmenuEntity.setOrden(mockValues.nextInteger());
		itemmenuEntity.setActivo(mockValues.nextByte());
		itemmenuEntity.setMenu(new MenuEntity());
		itemmenuEntity.getMenu().setId(mockValues.nextLong());
		
		// When
		Itemmenu itemmenu = itemmenuServiceMapper.mapItemmenuEntityToItemmenu(itemmenuEntity);
		
		// Then
		assertEquals(itemmenuEntity.getLink(), itemmenu.getLink());
		assertEquals(itemmenuEntity.getNombre(), itemmenu.getNombre());
		assertEquals(itemmenuEntity.getOrden(), itemmenu.getOrden());
		assertEquals(itemmenuEntity.getActivo(), itemmenu.getActivo());
		assertEquals(itemmenuEntity.getMenu().getId(), itemmenu.getMenuid());
	}
	
	/**
	 * Test : Mapping from 'Itemmenu' to 'ItemmenuEntity'
	 */
	@Test
	public void testMapItemmenuToItemmenuEntity() {
		// Given
		Itemmenu itemmenu = new Itemmenu();
		itemmenu.setLink(mockValues.nextString(45));
		itemmenu.setNombre(mockValues.nextString(45));
		itemmenu.setOrden(mockValues.nextInteger());
		itemmenu.setActivo(mockValues.nextByte());
		itemmenu.setMenuid(mockValues.nextLong());

		ItemmenuEntity itemmenuEntity = new ItemmenuEntity();
		
		// When
		itemmenuServiceMapper.mapItemmenuToItemmenuEntity(itemmenu, itemmenuEntity);
		
		// Then
		assertEquals(itemmenu.getLink(), itemmenuEntity.getLink());
		assertEquals(itemmenu.getNombre(), itemmenuEntity.getNombre());
		assertEquals(itemmenu.getOrden(), itemmenuEntity.getOrden());
		assertEquals(itemmenu.getActivo(), itemmenuEntity.getActivo());
		assertEquals(itemmenu.getMenuid(), itemmenuEntity.getMenu().getId());
	}

}